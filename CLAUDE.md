# Claude rules

## Pull requests
- NEVER merge a pull request. Do not call merge tools or run merge commands on
  PRs under any circumstances — leave merging to the user, even if asked to
  "merge" earlier in a session. Resolving conflicts and pushing branches is
  fine; the final merge is the user's to perform.
